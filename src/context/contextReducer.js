/*export const reducer = (state, action) => {
  switch (action.type) {
    case "inc":
      return (state = state + 1);
      break;
    case "dec":
      return (state = state - 1);
      break;
    case "string":
      return (state = typeof state == "number" ? "Thanks God" : "Wait");
      break;
    default:
      return state;
      break;
  }
};
*/